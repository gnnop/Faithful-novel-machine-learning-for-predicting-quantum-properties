name: mmp
channels:
  - defaults
  - conda-forge
dependencies:
  - pip=23.2.1
  - python=3.11.4
  - pip:
      - numpy
      - matplotlib
      - mendeleev
      - scipy
      - tqdm
prefix: /software/anaconda3/envs/mmp
